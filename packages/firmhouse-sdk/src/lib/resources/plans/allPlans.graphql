query allPlans($after: String, $before: String, $last: Int, $first: Int) {
  plans(after: $after, before: $before, last: $last, first: $first) {
    totalCount
    pageInfo {
      ...PageInfoFields
    }
    nodes {
      ...PlanFields
    }
  }
}

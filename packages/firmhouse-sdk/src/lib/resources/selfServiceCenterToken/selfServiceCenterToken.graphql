mutation CreateSelfServiceCenterLoginToken(
  $email: String!
  $returnUrl: String!
) {
  createSelfServiceCenterLoginToken(
    input: { email: $email, returnUrl: $returnUrl }
  ) {
    error
    status
  }
}
